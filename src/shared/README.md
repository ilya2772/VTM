# Shared modules

Pure types, schemas and deterministic utilities that are safe in both runtimes live under `src/shared`. This layer must not access environment variables, databases, cookies or other server-only APIs.
